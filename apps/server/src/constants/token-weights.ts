/**
 * Коэффициенты пересчета операций в эквивалентные AI-токены.
 *
 * Базис: 1 текстовый токен дешевой LLM (например gpt-4o-mini / gemini-2.5-flash ~$0.60/1M) = 1 токен баланса.
 * - TTS (OpenAI TTS ~$15/1M символов): 1 символ текста = 20 эквивалентных токенов.
 * - STT (Whisper ~$0.006/мин = $0.0001/сек): 1 секунда аудио = 150 эквивалентных токенов.
 */
export const TOKEN_WEIGHTS = {
  /** Множитель символов текста для TTS (1 символ = 20 токенов) */
  TTS_CHAR_MULTIPLIER: 20,
  /** Минимальное списание за один TTS запрос */
  MIN_TTS_TOKENS: 20,
  /** Множитель секунд аудио для STT (1 секунда = 150 токенов) */
  STT_SECOND_MULTIPLIER: 150,
  /** Минимальное списание за один STT запрос */
  MIN_STT_TOKENS: 100,
} as const
